import {createSlice, nanoid} from "@reduxjs/toolkit"

const initialState = {

}

export const methodSlice = createSlice({
    name: 'method',
    initialState,
    reducers: {
        
    }

})